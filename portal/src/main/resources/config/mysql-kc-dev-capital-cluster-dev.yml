spring:
  datasource:
    url: jdbc:mysql://192.168.214.93:3306/kakaodb?autoReconnect=true&useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&allowMultiQueries=true
    username: ${DB_USER:kakao}
    password: ${DB_PASSWD:kakao00!}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 30
  sql:
    init:
      mode: never
      platform: mysql
  jpa:
    database-platform: org.hibernate.dialect.MySQL57Dialect
    show-sql: false
    open-in-view: false
    defer-datasource-initialization: true
#    hibernate.hbm2ddl.auto: update
    hibernate:
      ddl-auto: update
    generate-ddl: true
    properties:
      hibernate:
        globally_quoted_identifiers: true
        format_sql: true
        use_sql_comments: true
        default_batch_fetch_size: 1000
        temp:
          use_jdbc_metadata_defaults: false