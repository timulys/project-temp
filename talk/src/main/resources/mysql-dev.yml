spring:
  datasource:
    url: jdbc:mysql://always-dev-db-mysql-cluster-rw.dbe.dktechin.in:3306/always?autoReconnect=true&useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&allowMultiQueries=true
    username: ${DB_USER:always_dev_service}
    password: ${DB_PASSWD:i9raq3JZlqK6E2hJTO@GfJan:@=CxW}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 30
    sql:
      init:
        mode: never
        platform: mysql
    jpa:
      database-platform: org.hibernate.dialect.MySQL57Dialect
      show-sql: false
      open-in-view: false
      defer-datasource-initialization: true
      hibernate:
        ddl-auto: update
      generate-ddl: true
      properties:
        hibernate:
          globally_quoted_identifiers: true
          format_sql: true
          use_sql_comments: true
          default_batch_fetch_size: 1000
          temp:
            use_jdbc_metadata_defaults: false
